<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Автопубликация объявлений
    |--------------------------------------------------------------------------
    |
    | true  — объявление владельца выходит на витрину сразу после создания.
    |          Менеджер видит его в админке и может снять постфактум.
    | false — объявление уходит в статус pending и ждёт одобрения.
    |
    | Решение заказчика от 17.09.2026: публикуем сразу. Премодерация тормозит
    | наполнение витрины, а на старте важнее, чтобы объявления появлялись.
    | Вся машинерия модерации остаётся на месте — включается одной строкой,
    | когда объёмы вырастут и появится поток мусора.
    |
    */

    'auto_publish' => (bool) env('LISTING_AUTO_PUBLISH', true),

    /*
    | Пост-контроль: объявления, опубликованные без проверки, всё равно
    | попадают в отдельную очередь админки — чтобы менеджер их просматривал
    | не блокируя владельца.
    */
    'flag_unreviewed' => (bool) env('LISTING_FLAG_UNREVIEWED', true),

];
