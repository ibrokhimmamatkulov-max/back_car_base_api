<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Автопубликация объявлений
    |--------------------------------------------------------------------------
    |
    | true  — объявление владельца выходит на витрину сразу после создания.
    |          Менеджер видит его в админке и может снять постфактум.
    | false — объявление уходит в статус pending и ждёт одобрения.
    |
    | Решение заказчика от 17.09.2026: публикуем сразу. Премодерация тормозит
    | наполнение витрины, а на старте важнее, чтобы объявления появлялись.
    | Вся машинерия модерации остаётся на месте — включается одной строкой,
    | когда объёмы вырастут и появится поток мусора.
    |
    */

    'auto_publish' => (bool) env('LISTING_AUTO_PUBLISH', true),

    /*
    | Пост-контроль: объявления, опубликованные без проверки, всё равно
    | попадают в отдельную очередь админки — чтобы менеджер их просматривал
    | не блокируя владельца.
    */
    'flag_unreviewed' => (bool) env('LISTING_FLAG_UNREVIEWED', true),

    /*
    |--------------------------------------------------------------------------
    | Срок жизни объявления
    |--------------------------------------------------------------------------
    |
    | Решение заказчика от 25.09.2026: объявление живёт 30 дней с момента
    | публикации, дальше снимается с витрины само — как на классических
    | досках объявлений. Отсчёт от published_at, а не от даты создания:
    | повторная публикация (после паузы или возврата из архива) обнуляет
    | published_at и даёт объявлению свежие 30 дней.
    |
    | Действует в двух местах:
    | 1. scopeVisibleOnShowcase() на модели — прячет просроченное с витрины
    |    немедленно, даже если сборщик ниже ещё не пробегал.
    | 2. Console\Commands\ExpireListings — переводит просроченное в статус
    |    archived, чтобы владелец увидел это в кабинете, а не только «вдруг
    |    исчезло с сайта». Требует рабочий `php artisan schedule:run` по
    |    крону; без него пункт 1 всё равно не даст просроченному висеть
    |    на публичной витрине.
    |
    | Легаси-записи без published_at (созданы до перехода на новую модель)
    | под это правило не попадают — у них нечего сравнивать со сроком.
    |
    */

    'lifetime_days' => (int) env('LISTING_LIFETIME_DAYS', 30),

];
